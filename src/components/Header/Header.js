import React from 'react';
import './Header.css'

// 함수형 컴포넌트
// state가 없고 life cycle이 필요없는 presentational component일 때 사용
const Header = () => (
  <div className="Header">
    POSTS
  </div>
)

export default Header;